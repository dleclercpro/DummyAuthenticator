import { Theme } from '@mui/material';
import { makeStyles } from 'tss-react/mui';

const useSpinnerStyles = makeStyles()(({ palette, spacing }: Theme) => ({
    root: {

    },
}));

export default useSpinnerStyles;