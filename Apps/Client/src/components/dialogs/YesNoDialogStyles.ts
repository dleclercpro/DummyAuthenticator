import { Theme } from '@mui/material';
import { makeStyles } from 'tss-react/mui';

const useYesNoDialogStyles = makeStyles()(({ palette, spacing }: Theme) => ({
    root: {

    },
}));

export default useYesNoDialogStyles;