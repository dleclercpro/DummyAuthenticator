export enum MIMEImageType {
    PNG = 'image/png',
    JPEG = 'image/jpeg',
    SVG = 'image/svg+xml',
    TIFF = 'image/tiff',
    Bitmap = 'image/bmp',
    Icon = 'image/x-icon',
    GIF = 'image/gif',
}

export enum MIMEApplicationType {
    JSON = 'application/json',
}